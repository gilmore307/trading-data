# Schemas

Machine-readable contract skeletons for `trading-data` task keys, completion receipts, source rows, and feature rows.

These schemas define the shared envelope fields. Feed/source-specific fields remain owned by the relevant module docs and tests until a reviewed registry-backed schema is accepted.
